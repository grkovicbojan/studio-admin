# Vite Responsive Landing

This project now runs as a Vite application (not raw HTML/CSS/JS files).

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```